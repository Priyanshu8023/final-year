# ── Core Data ──
pandas
numpy
scipy

# ── Data Sources (ingestion / training) ──
yfinance
requests

# ── Config & Serialization ──
python-dotenv
pyyaml
joblib
dill
tqdm
openpyxl

# ── ML Models ──
scikit-learn
xgboost
lightgbm

# ── Deep Learning (PyTorch only — TensorFlow not used) ──
torch

# ── Technical Analysis ──
ta

# ── NLP / Sentiment ──
nltk
textblob
vaderSentiment

# ── External Data APIs ──
fredapi
## alphavantage — disabled: its 'foil' dep fails on Python 3.14
## gdelt        — disabled: optional, not needed for inference

# ── Visualization ──
matplotlib
seaborn
plotly
mplfinance

# ── API Server ──
fastapi
uvicorn
python-multipart

# ── Monitoring (optional) ──
mlflow
evidently

# ── Notebooks ──
ipykernel

## After the project is completed then at last, Use it only once to install the package in editable mode and then comment it out 
#-e .